// THE NOW FILE (G3, 2026-07-10, Emilie's ruling): the one file to edit when
// life moves. NOW emits as the newest entry in the registry timeline, so the
// neural world's live red tip always answers "doing what?". (The About page's
// NOW module retired with the contact-sheet rebuild, Emilie 2026-07-12; the
// registry/CV tip is now this data's only renderer.) Bump `date` when
// editing; the registry validator enforces that NOW stays the newest dated
// thing in the record.
//
// draftCopy: every line below is drafted in Emilie's voice, unsigned.

export const NOW = {
  date: '2026-07', // 'YYYY-MM' · must remain the newest date in the registry
  building: 'the MaCAD thesis year, and the site you are standing in',
  reading: 'neuroaesthetics papers, faster than I can annotate them',
  thinking: 'what a facade knows about your cortisol',
} as const

// (The optional reading shelf was SKIPPED for G3 by Emilie, 2026-07-10;
// if it ever ships, its data belongs here beside NOW.)
